export const configuracionApp = Object.freeze({
  datos: Object.freeze({
    horario: "data/horario.csv",
    colores: "data/colores.csv",
    cambiosTemporales: "data/cambios-temporales.json",
  }),
});
