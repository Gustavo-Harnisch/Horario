# Datos

Los datos publicados permanecen en `Horario/data/` para conservar las rutas existentes de GitHub
Pages. Los módulos de `src/` los consumen a través de `core/config.js`; no se mantienen copias
duplicadas dentro de esta carpeta.
