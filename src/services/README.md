# Services

Los services se encargan de leer o guardar información externa.

- `csv.js` carga y transforma archivos CSV.
- `storage.js` lee y escribe datos en `localStorage`.

No renderices HTML aquí ni coloques reglas propias de una pantalla. Devuelve datos para que los módulos los utilicen.
